{
  "name": "mobile",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "devDependencies": {}
}
# Interface web fusionnée (restaurant + admin)

Ce dossier contient l'application React.js qui regroupe les interfaces restaurateur et administrateur.

- `/src/restaurant/` : pages et composants pour les restaurateurs
- `/src/admin/` : pages et composants pour les administrateurs
- `/src/shared/` : composants, hooks, helpers partagés

Initialise ce projet avec Vite ou Create React App.

