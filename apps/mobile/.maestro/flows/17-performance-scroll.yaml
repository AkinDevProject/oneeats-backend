# =============================================================================
# FLOW 17: Tests de performance et scroll
# =============================================================================
appId: com.oneeats.mobile
name: "17 - Performance & Scroll Tests"
tags:
  - performance
  - regression

---

# SETUP
- launchApp:
    clearState: true

- extendedWaitUntil:
    visible: ".*"
    timeout: 15000

- runFlow:
    when:
      visible: "Continuer sans compte"
    commands:
      - tapOn: "Continuer sans compte"

- extendedWaitUntil:
    visible: ".*proximit.*"
    timeout: 15000

# TEST 1: Scroll vertical
- scroll
- scroll
- scroll

- takeScreenshot: "17-scroll-down"

# TEST 2: Navigation rapide entre onglets (Accueil, Panier, Compte)
- tapOn: "Panier"
- tapOn: "Compte"
- tapOn: "Accueil"

- takeScreenshot: "17-tab-switch"

# TEST 3: Scroll rapide
- scroll
- scroll
- scroll

- takeScreenshot: "17-rapid-scroll"

# Verification finale
- extendedWaitUntil:
    visible: ".*proximit.*"
    timeout: 5000

- takeScreenshot: "17-final-stable"
